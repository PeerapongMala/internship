export const pathAsset = './assets/';

// export const pathAssetAbs = '.' + pathAsset;
// export const pathAsset = window.location.origin + pathAsset;

const config = {
  pagination: {
    itemsPerPage: 100,
    itemPerPageOptions: [10, 25, 50, 100],
    modalItemPerPage: 10,
  },
};

export default config;
