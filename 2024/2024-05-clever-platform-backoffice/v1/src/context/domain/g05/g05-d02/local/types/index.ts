export type TPagination = {
  page: number;
  limit: number;
  total_count: number;
};

export type TSchoolData = {
  school_id: string;
  school_code: string;
  school_name: string;
  school_image_url: string;
};
