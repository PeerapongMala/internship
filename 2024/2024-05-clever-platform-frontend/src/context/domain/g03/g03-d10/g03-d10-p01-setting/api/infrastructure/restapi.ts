import Account from '../group/account/restapi';
import { RepositoryPatternInterface } from '../repository-pattern';

const RestAPI: RepositoryPatternInterface = {
  Account,
};

export default RestAPI;
