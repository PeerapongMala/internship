import UploadHistory from '../group/upload-history/mock';
import { RepositoryPatternInterface } from '../repository-pattern';

const Mock: RepositoryPatternInterface = {
  UploadHistory,
};

export default Mock;
