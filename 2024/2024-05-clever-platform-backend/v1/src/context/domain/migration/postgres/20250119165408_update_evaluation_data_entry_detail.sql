-- +goose Up
-- +goose StatementBegin
create index evaluation_data_entry_sheet_id_index
    on grade.evaluation_data_entry (sheet_id);

create index evaluation_form_note_sheet_id_index
    on grade.evaluation_form_note (sheet_id);

-- alter table grade.evaluation_data_entry
--     drop column json_student_score_data;

alter table grade.evaluation_data_entry add created_at timestamp NOT NULL DEFAULT NOW();
alter table grade.evaluation_data_entry add created_by char(36) NOT NULL DEFAULT '';
alter table grade.evaluation_data_entry add updated_at timestamp NOT NULL DEFAULT NOW();
alter table grade.evaluation_data_entry add updated_by char(36) NOT NULL DEFAULT '';
--
-- CREATE TABLE "grade"."student_score_data" (
--                                               "id" INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
--                                               "data_entry_id" int NOT NULL,
--                                               "evaluation_student_id" int NOT NULL
-- );
--
-- CREATE TABLE "grade"."student_indicator_data" (
--                                                   "id" INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
--                                                   "student_score_data_id" int NOT NULL,
--                                                   "indicator_id" int,
--                                                   "value" decimal(8, 2)
-- );
--
-- ALTER TABLE "grade"."student_score_data" ADD FOREIGN KEY ("data_entry_id") REFERENCES "grade"."evaluation_data_entry" ("id");
--
-- ALTER TABLE "grade"."student_score_data" ADD FOREIGN KEY ("evaluation_student_id") REFERENCES "grade"."evaluation_student" ("id");
--
-- ALTER TABLE "grade"."student_indicator_data" ADD FOREIGN KEY ("student_score_data_id") REFERENCES "grade"."student_score_data" ("id");
--
-- ALTER TABLE "grade"."student_indicator_data" ADD FOREIGN KEY ("indicator_id") REFERENCES "grade"."evaluation_form_indicator" ("id");

-- +goose StatementEnd

-- +goose Down
-- +goose StatementBegin
drop index grade.evaluation_data_entry_sheet_id_index;
drop index grade.evaluation_form_note_sheet_id_index;
-- alter table grade.evaluation_data_entry add json_student_score_data text;
alter table grade.evaluation_data_entry drop column created_at;
alter table grade.evaluation_data_entry drop column created_by;
alter table grade.evaluation_data_entry drop column updated_at;
alter table grade.evaluation_data_entry drop column updated_by;
-- drop table "grade"."student_indicator_data";
-- drop table "grade"."student_score_data";
-- +goose StatementEnd
